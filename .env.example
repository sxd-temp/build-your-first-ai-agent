# Required: LLM provider — get a FREE key at https://aistudio.google.com/app/apikey
# (sign in with the same Google account, click "Create API key", paste it below)
GEMINI_API_KEY=

# Web search uses DuckDuckGo via the `ddgs` package — no key required.

# Optional: override default model (any LiteLLM-supported Gemini model)
# Examples: gemini/gemini-2.5-flash, gemini/gemini-2.5-flash-lite, gemini/gemini-2.5-pro
# MODEL_NAME=gemini/gemini-2.5-flash
